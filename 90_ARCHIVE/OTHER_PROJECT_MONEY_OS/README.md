# Money OS 包已移除（2026-09-07，Owner 批准）

这里原本放着 `Project_M_Master_Development_Package_v1.3.1_COMPLETE.zip`（422 KB）——
**另一个项目**的开发包，在 M0 引导时随文档一起被提交进来，从此没有任何东西读过它。

**DEC-005 把它封存在范围之外**，那条决定继续有效：本项目的任何代码或文档都不依赖它，
也不应该去解压或阅读它。

移除的理由不是它占地方，而是**这个仓库现在是公开的**，而它不属于这个项目。

## ⚠️ 它仍然在 git 历史里

`git rm` 只是让它从今往后的检出中消失。它仍然存在于 **commit `e62e073`**
（M0 Bootstrap）中，任何人只要知道那个 SHA 就仍然能从 GitHub 下载。

**要真正清除，必须重写历史并强制推送 `main`**——所有 commit SHA 都会改变，
任何已有的克隆都要重新克隆。命令在 `DECISIONS.md` 里，没有 Owner 明确同意不会执行。
