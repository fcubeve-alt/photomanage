# 上线准备（50_LAUNCH）

**建立于 2026-09-11**，起因是 Owner 的一句反问：

> "apple 开发者账号由我来负责，前面的支付、账号、订阅、App Store 商品、退款流程、
> 隐私政策、服务条款、支持渠道、崩溃上报、数据迁移方案，这些你不应该先做好吗"

**这句话是对的。** 之前我把这十项统统归到"等真机验证之后再说"，而其中大部分和真机、
和开发者账号都没有任何关系——它们只是没被做。本目录是补上的那一部分。

---

## 十项的真实状态

| # | 项目 | 状态 | 在哪里 | 还差什么 |
|---|---|---|---|---|
| 1 | **隐私政策** | ✅ 已写 | `PRIVACY_POLICY.md`，并已编进 App 内《支持 → 隐私政策》 | Owner 填支持邮箱；托管到一个 URL |
| 2 | **服务条款** | ✅ 已写 | `TERMS_OF_SERVICE.md`，同上 | Owner 确认第 6、8 条（免责与适用法律）；托管 |
| 3 | **支持渠道** | ✅ 方案已定 + App 内入口已实现 | `SUPPORT.md`，`40_APP/PVM/UI/SupportView.swift` | Owner 定一个支持邮箱 + 一个 GitHub Pages 支持页 |
| 4 | **崩溃上报** | ✅ 已实现 | `40_APP/PVM/Support/CrashReporter.swift` | 无。设计上不接任何第三方 SDK——见下 |
| 5 | **数据迁移方案** | ✅ 已实现 + 有测试 | `40_APP/PVMCore/Sources/PVMCore/Migration.swift`，`DATA_MIGRATION.md` | 无。顺带修掉一个真实的顺序 bug |
| 6 | **支付 / 订阅** | ⏸ 代码已写、可编译，**流程未验证** | `40_APP/PVM/Store/`，`40_APP/PVM.storekit` | 需要在 Xcode 里跑一次模拟购买；`.storekit` 文件格式也尚未打开验证过 |
| 7 | **退款流程** | ⏸ 代码已写，**未验证** | `Purchases.swift` 的 `requestRefund()` → Apple 官方退款表单 | 同上，且需要一笔真实交易才能端到端验证 |
| 8 | **App Store 商品** | ⏸ 标识符与配置已定 | `APP_STORE_SUBMISSION.md` 第 7 节 | **必须有开发者账号**才能创建 |
| 9 | **账号体系** | ✅ 决定是"不做" | `PRIVACY_POLICY.md` 第 6 节 | 无——见下 |
| 10 | **定价** | ⏸ 提案已写，**待 Owner 裁决** | `PRICING_PROPOSAL.md` | 一个决定 |

## 三件需要解释的事

**为什么"账号体系"的答案是不做。**
这个产品没有自己的服务器，也没有任何把数据发给我们的代码（唯一的对外通路是 Apple 的
StoreKit，属于 Apple）。加一个账号，意味着为了卖一样根本不需要邮箱的东西
而去收集一个邮箱地址——它会把"我们不持有你的任何数据"这句话变成假的。购买绑定在 Apple ID
上，换设备用 `AppStore.sync()` 恢复，不需要账号。**这是一个决定，不是一项缺失。**

**为什么崩溃上报不接 Crashlytics / Sentry。**
它们的工作方式就是把报告发到别人的服务器。竞品拆解（`20_TIER0/evidence/COMPETITOR_PRICING_MATRIX.md`）
记录了这个品类里的标准做法：用户付了 $7.99/周之后，元数据仍然经由 6 个以上追踪 SDK 外传，
而隐私标签写着"未关联到你"。装一个那样的 SDK，再写一份"不收集任何数据"的隐私政策，
就是同一个谎言换了个名字。所以崩溃记录写在本机，下次启动时**把全文给用户看**，
由用户自己决定要不要用自己的邮件发出来。**代价是我们会漏掉绝大多数崩溃**，
这是为了那句话是真的所付的价。

**为什么支付代码可以在没有账号的情况下写完 —— 以及它到目前为止证明了什么。**
`PVM.storekit` 是一个本地 StoreKit 配置文件，购买、恢复、取消、退款的全部代码路径 *可以* 在模拟器里跑，不需要开发者账号。

**但截至 2026-09-12，这条路径一次都没有真正跑过。** 已经证明的只有"它能编译"和"付费边界当前为空"。没跑过的包括：`.storekit` 配置是否能被 Xcode 加载、模拟购买是否成功、权益是否解锁、恢复购买、退款后是否重新上锁、Ask-to-Buy 挂起。第二次审计对这一点的批评是对的：之前那句"模拟器可跑通"把"能跑"写成了"跑过"。

它需要一台 Mac：在 Xcode 里 Edit Scheme → Run → Options → StoreKit Configuration 选中 `PVM.storekit`，然后手动走一遍。列为上线前必做项。

## 仍然只有 Owner 能做的四件事

1. **Apple Developer Program（$99/年）** —— 硬门槛。Owner 已接下。
2. **一个支持邮箱**（建议别用个人主邮箱，见 `SUPPORT.md` 第 3 节）。
3. **两个公开 URL**（支持页 + 隐私政策页）—— GitHub Pages 即可，零成本。
4. **定价裁决** —— `PRICING_PROPOSAL.md`，三选一。

## 仍然是"没有数据"的部分

本目录**不改变**上一次判断里那张表：A3 生存性、删除→恢复、真实照片准确率、A1 规模，
四项全部是零数据，而这四项要装到真机上才有答案。

**这十项做完不等于可以上线。** 它做完的是：当真机验证通过、账号到位的那一刻，
不再有任何非工程的东西挡在中间。
