__pycache__/
*.pyc
.DS_Store
Thumbs.db
~$*
*.tmp
tmp/
logs/

# T0-C2 collected data. emails.jsonl contains real people's addresses and must
# NEVER be committed. events.jsonl is study data, kept out of git so a smoke test
# can never contaminate a real run.
20_TIER0/landing/events.jsonl
20_TIER0/landing/emails.jsonl
20_TIER0/study_assets/library/
20_TIER0/study_assets/_smoke*/

# engine working files
30_ENGINE/**/*.sqlite
30_ENGINE/**/*.sqlite-wal
30_ENGINE/**/*.sqlite-shm
# The ablation arm writes a second catalogue beside the first.
30_ENGINE/**/*.sqlite.ablation
30_ENGINE/**/*.sqlite.ablation-wal
30_ENGINE/**/*.sqlite.ablation-shm
